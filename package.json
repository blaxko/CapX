{
  "name": "capx",
  "version": "0.1.0",
  "private": true,
  "description": "CapX — on-chain spend enforcement for AI agent fleets. OKX.AI Agent Service Provider.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xlayerTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network xlayer"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6",
    "solc": "^0.8.20"
  }
}
